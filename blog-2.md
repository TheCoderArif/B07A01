title: The four pillars of OOP—Inheritance, Polymorphism, Abstraction, and Encapsulation—help manage logic and reduce complexity in large-scale TypeScript projects.

Introduction: The OOP itself a very updated programming because of Four pillers like Inheritance, Polymorphisom, Abstration and Encapsulation.

Body: 

1. Inheritance: In Programming we can use a method in another portion of a function by using inheritance. it save time , make code more sufficient and mor effective with saving data.

2. Polymorphism: Plymorphism is the greatest thing of object oriented programming.By this method we can use a programm in  a very effective way. We can call something which will work as the object. call one method but it will work various as the instruction of the object.

3. Abstraction: Abstraction is a feature which show only the essential part of the object.

4. Encapsulation: Encapsulation is a way to hide something from the Object. it gives access to those methods which inside of the Object

Conclusion: The four piller of OOP is very effective and time saving which drove the programming to the next level.