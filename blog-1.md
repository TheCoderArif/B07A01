
1. Title: Why is 'any' labeled a 'type safety hole'

Introduction: Typescript used to declare the type of data. Not to be confused.

Body: Typescript was created to minimize complexity using the type of data.But using 'any' type in the programm it cannot get the info about data. by using 'any' type in typescript , there are no deffirence between typescript and javascript. 
The type 'any' can be used any kind of data like undefined or null.

conclusion: It's like a gap using 'any' type in typescript . that's why it's called 'type safety hole'.







2. Title: 'unknown' is the safer choice for handling unpredictable data.

Introduction:In a large programm we have to work with any kind of data like string , number, boolean , undefined or null. 

Body:In a case, we don't know what kind of data will user input. that's why we have to use 'if else' logic to handle those kind of data . but we can use 'unknown' as a  data type which can be handle all kind of data.

conclusion: because of universal declaring ability 'unknown' is the safer choice for handling unpredictable data.








3. Title: Type Narrowing

Introduction: Type narrowing is the best way to save time , code and complexity in the code.

Body: Type narrowing is a way to declare type and use it again and again without express all the syntex. Also it saves complexity and save time.

Conclusion: Type narrowing is the way to make programm more effective and smooth. that's why it's very important tool in typescript.